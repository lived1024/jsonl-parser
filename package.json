{
  "name": "jsonl-parser",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run generate:sitemap && vite build && npm run optimize:build",
    "build:only": "vite build",
    "build:fast": "npm run generate:sitemap && vite build",
    "build:production": "npm run check:compliance && npm run generate:sitemap && vite build && npm run optimize:build",
    "generate:sitemap": "node scripts/generate-sitemap.js",
    "optimize:build": "node scripts/optimize-build.js",
    "check:compliance": "node scripts/check-ad-compliance.js",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@types/marked": "^5.0.2",
    "highlight.js": "^11.11.1",
    "lucide-vue-next": "^0.447.0",
    "marked": "^16.1.2",
    "pinia": "^2.1.7",
    "vue": "^3.4.0",
    "vue-i18n": "^11.1.11",
    "vue-router": "^4.2.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.5.0",
    "@vue/test-utils": "^2.4.6",
    "jsdom": "^26.1.0",
    "typescript": "^5.0.2",
    "vite": "^4.4.5",
    "vitest": "^3.2.4",
    "vue-tsc": "^1.4.2"
  }
}
